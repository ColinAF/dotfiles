#!/usr/bin/env bash

# make symlinks of my dotfiles and put them in $HOME
FILES="vimrc bashrc bash_profile tmux.conf zshrc gitconfig Xresources"

for file in $FILES
do
	echo "Symlinking $file"
	rm ~/.$file
	ln -s ${PWD}/$file ~/.$file

done 

